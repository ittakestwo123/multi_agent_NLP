# Example environment configuration for multi_agent_NLP project
# Copy this file to .env and fill in your own keys.

# --- Core LLM settings ---
OPENAI_API_KEY=
# Optional: use a compatible base URL (DeepSeek / api.chatanywhere / etc.)
OPENAI_BASE_URL=https://api.chatanywhere.tech/v1
LLM_MODEL=gpt-4o-mini

# Optional web search (SerpAPI)
SERPAPI_API_KEY=

# Embedding model name (only when your base URL supports embeddings)
EMBED_MODEL_NAME=text-embedding-3-small

# Enable interactive mode (1 to enable, 0 to disable)
ENABLE_INTERACTIVE=0

